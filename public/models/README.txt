Drop GLB files in this folder with exact file names:

1) sofa.glb
2) bed.glb
3) sink.glb
4) toilet.glb
5) door-frame.glb
6) window-frame.glb

Notes:
- Keep file names exactly same (case-insensitive on Windows, but keep exact spelling).
- Recommended size per model: under 10 MB for smooth dev experience.
- After adding files, restart dev server and refresh browser.
